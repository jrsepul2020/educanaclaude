---
interface Props { tone?: 'brand' | 'amber' }
const { tone = 'brand' } = Astro.props;
---
<p class={`text-(length:--text-eyebrow) font-semibold uppercase tracking-[0.16em] ${tone === 'amber' ? 'text-amber' : 'text-brand-ink'}`}>
  <slot />
</p>
